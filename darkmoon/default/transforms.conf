#
# Darkmoon transforms
#
# The target allowlist used by the "Send to Darkmoon" alert action. Only hosts
# present in this lookup may ever be sent for a retest/campaign. This is the
# hard boundary against out-of-scope / arbitrary scanning.
#
[darkmoon_targets_lookup]
filename = darkmoon_targets.csv
case_sensitive_match = false
